import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Breaking Bad characters',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
